




def NNDrawer(object):

	def __init__(self, frame, net):
		self.x0 = frame.x0
		self.y0 = frame.y0
		self.width = frame.width
		self.height = frame.height
		self.net


	def update():
		neuronSpacing = (self.width - nCount * 2 * r) // (nCount + 1)
		if(neuronSpacing < 2 * r):
			do something
		


	def makeLine()